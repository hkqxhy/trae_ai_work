# 租房雷达项目审查与改进建议

审查日期：2026-07-14  
审查范围：前端交互、信息架构、数据模型、本地规则、AI 接入、Node 服务、测试、部署与可扩展性  
审查结论：当前项目已经是一个完成度较高的演示型 MVP，但还不是可以放心长期使用的产品版本。

> 本文件只记录审查意见和后续建议，本次没有修改运行代码。

## 1. 我的设计判断

Reading this as：面向陌生城市租房者的决策工作台，服务对象是需要快速核验房源、比较候选并降低签约风险的普通租客，视觉方向适合克制、可信、信息层级清晰的产品化工具。

建议继续保持当前的设计基调，不再增加更多装饰性风格。后续的设计重点应该从“看起来更漂亮”转向：

- 让用户始终知道自己处于租房决策的哪一步。
- 让每个分数都能追溯到输入、规则和缺失信息。
- 让线上发现的风险自然进入线下看房和合同核验。
- 让用户可以保存、恢复、导出和分享自己的决策证据。

## 2. 当前做得好的地方

### 产品层面

1. 闭环方向正确。需求画像、房源扫描、候选对比、看房清单、沟通助手和合同检查形成了一条完整链路。
2. 本地规则和 AI 的职责分得比较清楚。README 已经明确“本地规则是稳定基线，AI 是补充层”，对高风险场景是正确的产品取舍。
3. 结果不是只给一个结论，而是同时提供风险原因、证据、追问和下一步动作，这比单纯的“安全 / 不安全”更可信。
4. Mock 模式使演示不依赖 API Key，适合比赛展示、离线开发和服务异常降级。
5. 沟通助手明确不会自动发送，也不会替用户承诺付款或签约，风险边界意识比较好。

### 工程层面

1. TypeScript 类型覆盖了主要前端业务对象，前后端 AI 结果也有独立模型。
2. AI 输出经过服务端归一化，避免直接信任模型返回的任意 JSON。
3. 服务端有请求 ID、超时、基础限流、敏感信息脱敏和 Mock 合约测试。
4. 图片 Object URL 在页面卸载、替换示例、重新上传和清空时有释放逻辑，说明已经考虑了浏览器资源生命周期。
5. 当前视觉刷新没有破坏页面信息架构，保留了已有交互路径和本地存储行为。

## 3. 最重要的总体判断

当前项目的主要风险不是“功能太少”，而是“功能承诺已经超过真实能力”。最典型的是房源图片和链接：界面将它们放进“多模态扫描”，但现在实际发送给服务端的只是图片文件名、大小和 MIME 类型，链接也没有被服务端抓取或解析。

因此下一阶段不应优先继续堆新页面，而应先完成下面三件事：

1. 把产品实际能力和页面文案对齐。
2. 给关键分数和风险结果建立可验证的证据链。
3. 把 API、存储和测试从演示级提升到可持续迭代级。

## 4. P0：建议优先修复的问题

P0 表示会直接影响产品可信度、用户数据安全或演示完整性的问题。

### P0-1：多模态能力目前存在明显的承诺与实现偏差

实施状态：已接入真实视觉输入链路。浏览器会压缩图片并生成受控 data URL，服务端校验后以 `image_url` 内容块传给 `QWEN_VL_MODEL`；候选房源持久化时会剥离图片内容，避免写入 localStorage。Mock 模式仍会明确标记不会真实读取图片。

证据：

- `src/components/ScanPage.tsx:247-291` 允许上传图片。
- `src/api/aiClient.ts:30-34` 发送图片时只发送 `name`、`size`、`type`。
- `server/services/listingAiService.js:14-34` 只校验图片数量、大小和类型，没有读取图片内容。
- `src/utils/candidateFactory.ts:63-68` 保存候选时将 `previewUrl` 清空。

实际效果是：图片可以用于浏览器预览，也可以作为用户手动填写备注的上下文，但当前并没有真正的 OCR、视觉模型分析或图片证据持久化。

原审查建议：

- 短期诚实降级：把“图片扫描”改成“图片辅助核验”，明确提示“当前需要手动补充图片文字和重点，暂不读取图片内容”。
- 中期补齐能力：浏览器端压缩图片，服务端接收受控的 base64 或对象存储地址，调用支持视觉输入的模型，并在结果中保存 `imageId`、OCR 文本、引用区域和模型版本。

本次采用中期方案的第一步：当前保存模型版本和输入模态，尚未持久化 OCR 文本、图片区域引用或原图证据包，这些仍属于后续 P1/P2 增强项。

验收标准：用户上传图片后，结果必须能明确回答“模型是否看到了图片内容”；如果没有看到，不应该把结果描述成多模态分析。

### P0-2：候选房源管理页面存在，但没有进入主流程

证据：

- `src/components/CandidatesPage.tsx` 实现了候选列表、删除和清空候选。
- `src/data/routes.ts` 没有候选管理路由。
- `src/App.tsx:259-289` 的 `PageContent` 没有渲染 `CandidatesPage`。

这会造成一个明显的产品断裂：用户可以在扫描页保存候选，但没有一个明确的“候选房源管理”入口；删除和清空能力也处于不可达状态。

建议：

- 增加“候选房源”一级导航，或者将 `CandidatesPage` 的全部能力合并进“房源对比”页。
- 如果决定不保留独立页面，应删除死代码和未使用的组件，避免以后误以为该页面已经接入。
- 删除候选时同时清理该候选对应的看房进度和备注，避免 localStorage 中留下孤儿数据。

### P0-3：评分语义需要更严格地定义

证据：

- `src/utils/listingRiskScanner.ts:407-408` 使用固定权重和关键词扣分生成分数。
- `src/utils/comparisonScoring.ts:13-18` 将风险、预算、通勤和完整度按固定比例合成。
- 缺失字段在 `src/utils/comparisonScoring.ts:27-60` 中会得到 45 分左右的可比较分数。

目前“可信度评分”“推荐分”“风险分”之间容易让用户误解。更严重的是，缺失通勤、费用或首期成本的房源仍然可以和完整房源一起排名。

建议：

- 明确区分 `riskScore`、`fitScore`、`completenessScore` 和 `confidence`。
- 缺失关键字段时显示“信息不足，暂不建议排序”，而不是用一个看似精确的 45 分代替。
- 在结果区域展示分数构成和缺失字段，而不是只展示一个总分。
- 为固定权重建立测试样例和基准数据，至少覆盖低价高风险、预算内高完整度、通勤超限和信息不足四类候选。
- AI 混合分的权重只在服务端定义一份，前端读取服务端返回的策略版本，避免前后端配置漂移。

### P0-4：公共 Render 服务的 API 边界还不够稳固

证据：

- `server/index.js:139-158` 的三个 AI 接口可以被公开调用。
- `server/middleware/rateLimit.js:3-20` 使用进程内 Map 限流。
- `server/index.js:55` 直接使用请求头中的 `x-forwarded-for` 作为限流标识。
- `server/index.js:28-51` 在 HTTP 层没有对请求结构做 schema 校验。

对于比赛演示基本够用，但公开部署后存在几个问题：

- 限流在服务重启后清零，多实例时不共享。
- 客户端可能伪造 `x-forwarded-for`，导致限流绕过或误伤。
- 服务层只做了部分字段截断，`profile` 和 `localRuleResult` 仍然可以被客户端任意构造。
- 没有按用户、会话或匿名设备区分配额，也没有成本上限。

建议：

- 在 HTTP 入口增加显式请求 schema 和字段上限。
- 在 Render 这种反向代理环境中只信任平台传递的客户端 IP，或使用经过验证的代理头。
- 生产环境使用 Redis / KV / 网关限流，至少为匿名请求增加短期 token 配额。
- 增加总请求大小、单字段长度、数组长度、AI 输出字符串长度和单次模型预算限制。
- 给 AI 端点增加简单的服务端观测：请求次数、耗时、超时、错误码、模型和 token 使用量。

### P0-5：静态文件路径校验建议改为基于 `resolve` 的安全判断

证据：`server/index.js:95-98` 使用 `join` 后再用 `filePath.startsWith(DIST_DIR)` 判断路径是否位于静态目录。

这种前缀判断没有目录分隔符边界，遇到相邻前缀目录时不够严谨。建议使用 `resolve(DIST_DIR, cleanPath)`，再判断结果等于 `DIST_DIR` 或以 `${DIST_DIR}${sep}` 开头，并拒绝包含非法路径的请求。

这不是当前演示中已经确认的可利用漏洞，但属于公开静态服务器应尽快消除的防御性问题。

## 5. P1：建议在下一轮产品迭代中解决

### P1-1：把状态从“页面状态”升级成“决策上下文”

目前 `src/App.tsx:93-95` 只在根组件维护画像和候选，页面本身通过 props 接收；同时 active route 只是内存状态，刷新页面会回到工作台。

建议建立一个轻量的 `DecisionContext` 或 reducer，统一管理：

- 当前画像版本。
- 当前选中的候选房源。
- 当前扫描结果。
- 当前看房任务进度。
- 当前合同检查结果。
- 当前最后一步和下一步动作。

同时将页面状态写入 URL hash 或路由，做到刷新后恢复当前页面，链接可以直接打开某个工作步骤。

### P1-2：统一存储层，增加版本迁移和错误处理

证据：`src/utils/profileStorage.ts`、`candidateStorage.ts`、`viewingChecklistProgressStorage.ts` 和 `viewingNotesStorage.ts` 各自直接读写 localStorage。

当前问题：

- `localStorage.setItem` 没有捕获容量异常。
- 数据格式只做浅校验，没有 schema version migration。
- 候选、清单和备注是多套 key，删除候选不会自动级联清理。
- 多标签页之间不会同步。
- 重要内容仍然只保存在单台设备上。

建议分阶段处理：

1. 先抽象 `storage` 适配器，统一 try/catch、版本号和迁移入口。
2. 为候选、备注和清单建立 `deleteCandidateCascade`。
3. 增加 `storage` 事件同步和“本地数据导出 / 导入”。
4. 需要跨设备时再引入后端账户和数据库，不要直接把 localStorage 逻辑散落在组件中。

### P1-3：扫描页需要真正的“证据链”

当前结果有风险标题、理由和追问，但没有保存用户当时输入的具体证据片段位置，也没有在候选详情中显示“这个风险来自哪段文案”。

建议让每条风险包含：

- `ruleId` 或 `modelRiskId`。
- 原文引用。
- 原文起止位置或引用片段。
- 触发规则 / 模型版本。
- 首次发现时间。
- 用户是否已核验。
- 用户上传或记录的补充证据。

这样可以把“风险提示”升级为“可复核的证据卡”，也方便后续重新扫描后做差异比较。

### P1-4：链接输入目前更像链接文本输入，而不是链接分析

`src/components/ScanPage.tsx` 将链接和文本一起交给本地规则，服务端也只收到 URL 字符串，没有抓取网页内容、解析平台结构或处理登录 / robots / 反爬失败。

建议：

- 页面文案改成“链接来源识别与手动补充”，直到真正实现抓取。
- 如果实现抓取，放在服务端，增加超时、大小上限、内容类型限制、SSRF 防护、robots 和失败降级。
- 把“URL 本身的风险”和“URL 页面内容的风险”拆成两个结果来源。

### P1-5：对比页要把“不可比”作为一等状态

目前对比表允许用户在一张表中补字段，但未填写字段仍会参与推荐。建议：

- 对关键字段显示“待补充”，不要只显示空输入框。
- 至少缺少月成本或通勤时，标注“信息不足”。
- 在顶部显示“可比较候选数 / 总候选数”。
- 支持按风险、月成本、通勤、完整度排序。
- 显示评分版本和规则权重，提供“为什么排在这里”的展开说明。

### P1-6：看房清单需要更强的现场使用体验

当前 `ViewingChecklistPage` 已经有通用清单、定制清单、勾选进度和备注，产品方向正确，但现场使用时还缺：

- 大触控目标和更明显的完成反馈。
- “只看未完成”筛选。
- 按类别折叠 / 展开。
- 现场拍照或上传证据。
- 一键生成“待追问列表”。
- 网络不稳定时的离线可用保证。
- 结束时生成一份候选结论，而不是只保存备注。

### P1-7：合同检查需要增加“原文定位”和版本差异

目前 `contractRiskScanner.ts` 主要依赖正则和关键字，能快速演示，但长合同中容易出现：

- 同一条款被多次匹配。
- 否定语句或例外条款被误判。
- 证据文本不够精确。
- 用户修改合同后无法知道风险是否降低。

建议增加文本分段、条款编号、原文区间和规则命中位置，并允许用户粘贴两个版本做差异对比。AI 只能补充解释，不应替代本地证据定位。

### P1-8：沟通助手可以增加事实边界检查

现在的回复生成已经有风格、目标、识别信号和避免表述。下一步可以增加发送前检查：

- 是否包含未被用户确认的金额。
- 是否包含承诺、威胁或法律断言。
- 是否把“希望对方确认”误写成“对方已经确认”。
- 是否超过平台消息长度。
- 是否包含手机号、身份证号、微信号等敏感信息。

这会让 AI 回复从“生成文本”升级成“生成 + 风险审校”。

## 6. 前端架构与体验改进

### 6.1 `App.tsx` 责任过重

`src/App.tsx` 同时承担路由状态、画像存储、候选存储、Demo 数据构造、页面选择和工作台渲染。短期可维护，继续加功能后会快速变成变更高风险文件。

建议拆成：

```text
src/
  app/
    AppShell.tsx
    routeConfig.ts
    DecisionProvider.tsx
  components/
    navigation/
    layout/
    feedback/
  pages/
    OverviewPage.tsx
    ProfilePage.tsx
    ScanPage.tsx
    CandidatesPage.tsx
    ComparisonPage.tsx
    ViewingChecklistPage.tsx
    NegotiationPage.tsx
    ContractPage.tsx
```

不一定要立即引入路由库，但应先把页面状态和业务动作从根组件中抽出。

### 6.2 CSS 目前有明显的叠加覆盖层

`src/styles.css` 现在包含原始样式、视觉刷新覆盖层和后续对齐覆盖层。这样可以快速迭代，但长期会出现：

- 同一个选择器在多个位置定义。
- 断点之间的优先级不容易推断。
- 旧设计变量仍然保留，实际值由后面的规则覆盖。
- 新页面很难判断应该复用哪一套组件规则。

建议下一轮将 CSS 收敛为：

1. 设计令牌。
2. 基础元素和表单。
3. 布局组件。
4. 页面组件。
5. 断点和 reduced motion。

同时删掉已失效的旧规则，而不是继续在文件底部追加覆盖。

### 6.3 字体加载与实际设计令牌不一致

证据：`index.html:9-13` 仍加载 Fraunces、Sora 和 Noto Serif SC；当前 `src/styles.css:1-3` 以及后续视觉刷新实际使用 Avenir Next 和系统中文字体。

建议删除未使用的 Google Fonts 请求，或者明确选定并自托管一套字体。这样可以减少外部请求、避免隐私和网络依赖，也可以避免首屏字体切换造成布局变化。

### 6.4 无真正的路由和可分享链接

当前导航通过 `useState` 切换页面，刷新后回到工作台，浏览器后退和前进也不能恢复页面。建议使用 URL hash 或轻量路由：

- `/profile`
- `/scan`
- `/compare`
- `/checklist`
- `/negotiate`
- `/contract`
- `/demo`

候选详情可以使用 `?candidate=...`。这会让 Render 演示、问题反馈和用户分享都更可靠。

### 6.5 可访问性还需要系统化检查

当前已经有部分 `aria-label`、`aria-pressed`、`aria-live` 相关意识，但还不完整。建议重点补齐：

- `mode-tabs` 使用真正的 `role="tab"`、`aria-selected` 和键盘左右切换。
- 主导航给当前项增加 `aria-current`。
- AI 加载、错误和保存成功状态使用 `aria-live="polite"`。
- 图片上传显示格式、大小、数量限制。
- 每张已上传图片提供单独删除按钮。
- 表格在移动端提供卡片视图或明确的横向滚动提示。
- 确认删除、清空候选和重置进度等不可逆动作。
- 统一键盘焦点样式，并使用 axe 或 Lighthouse 做自动检查。

## 7. 服务端和 AI 层改进

### 7.1 在服务入口增加 schema 校验

建议为三个 API 分别定义输入 schema：

- `listing-analysis`：模式、文本、URL、图片元数据、画像和本地结果。
- `negotiation-reply`：消息、目标、风格和本地识别结果。
- `comparison-explanation`：画像、候选列表、候选评分和扫描摘要。

schema 需要限制：字符串长度、枚举、候选数量、数组长度、数字范围和未知字段。当前归一化主要集中在 AI 输出，对输入的边界不够严格。

### 7.2 统一 AI 结果的版本与可追溯性

现在结果已经包含 provider、model、requestId 和 promptVersion，这是很好的基础。建议进一步保存：

- 输入摘要哈希，而不是敏感原文。
- 本地规则版本。
- 评分权重版本。
- 生成时间和耗时。
- 是否发生过重试或降级。
- 用户最终是否采纳 / 忽略该建议。

这样可以回答“这次结果为什么和上次不同”，也方便调整规则后做回归评估。

### 7.3 提示词要把不可信输入作为数据隔离

当前提示词将用户提供的房源文案、聊天记录和合同文本直接放入 JSON。建议在 prompt 中明确：这些内容是不可信数据，只能作为被分析材料，不得覆盖系统指令；对长文本分段并标识来源，避免房源文案里的提示注入影响模型行为。

### 7.4 归一化结果要有输出上限

`server/schemas/aiSchemas.js` 会过滤类型和候选 ID，但没有对数组数量和字符串长度做统一上限。建议对风险数、追问数、理由数、每个字符串长度和总 JSON 大小做限制，避免异常模型输出造成前端撑爆、日志放大或成本增加。

### 7.5 增加安全响应头和更严谨的静态服务器防护

建议增加：

- `Content-Security-Policy`。
- `X-Content-Type-Options: nosniff`。
- `Referrer-Policy`。
- `Permissions-Policy`。
- HTTPS 环境下的 `Strict-Transport-Security`。

同时修复静态路径解析，避免依赖简单字符串前缀判断。

## 8. 测试、质量和发布流程

### 当前验证结果

- TypeScript 构建通过：`node node_modules/typescript/bin/tsc -b`
- Vite 生产构建通过：`node node_modules/vite/bin/vite.js build`
- Mock AI 合约测试通过：`npm run test:ai:mock`
- Live AI smoke test 只有在配置 Qwen Key 时才会运行，未配置时会跳过。

### 主要缺口

`package.json` 当前只有构建和 AI smoke 脚本，没有 lint、前端单元测试、端到端测试或可访问性测试。建议增加：

1. 本地规则单元测试：房源风险、合同风险、沟通场景、候选评分。
2. 存储迁移和异常测试：坏 JSON、旧版本、localStorage 满、候选删除级联。
3. React 组件测试：输入、保存、取消 AI、错误、空状态、候选切换。
4. Playwright E2E：完整 Demo 流程、移动端导航、图片上传、刷新恢复。
5. axe / Lighthouse：键盘可用性、颜色对比、表单标签、首屏性能。
6. GitHub Actions：安装、类型检查、构建、Mock 测试、依赖审计。

### 发布配置问题

- `demo/render.yaml` 位于 `demo/` 目录，不是仓库根目录。新建 Render Blueprint 时需要确认 Blueprint 文件路径，否则 Render 默认不会自动发现它。
- `.env.example:5` 设置了 `NODE_ENV=production`，而本地 Vite 开发时会出现“NODE_ENV=production is not supported in the .env file”的警告。建议拆分 `.env.local`、`.env.production` 或不要让前端开发和服务端生产配置共用同一个 `.env`。
- `package.json` 没有锁定 Node 版本。建议增加 `engines` 或 `.nvmrc`，避免 Render 和本地使用不同 Node 版本。
- Render Free 服务会休眠，冷启动和 AI 超时应该在界面中明确提示，而不是让用户以为页面卡住。

## 9. 值得增加的产品亮点

### 9.1 租房决策档案

把一个候选房源变成一份可持续更新的“决策档案”：

```text
需求画像
  -> 房源原始材料
  -> 本地风险扫描
  -> AI 补充分析
  -> 沟通记录和追问
  -> 看房证据和清单
  -> 合同检查
  -> 最终决定与理由
```

这会把当前分散的页面真正串成产品核心，而不是几个独立工具的集合。

### 9.2 真实月成本计算器

除月租外，统一计算：押金占用、中介费、服务费、管理费、水电网费、通勤成本和一次性搬家成本，输出：

- 首期需要准备多少钱。
- 真实月均成本是多少。
- 低价房源是否因为额外费用反而更贵。
- 不同付款周期的现金流压力。

这是比单纯“租金对比”更有辨识度的亮点。

### 9.3 风险证据墙

每个风险都显示“发现于哪段原文、需要谁提供什么证据、现场如何核验、核验后状态是什么”。用户可以把风险状态从“未确认”改成“已确认 / 已解释 / 不接受”。

### 9.4 不确定性而不是伪精确分数

在信息不足时显示：

- 当前分数。
- 影响最大的缺失字段。
- 补齐哪个字段最可能改变排序。
- 当前建议的可信度范围。

这比把所有房源都强行压成一个 0 到 100 的数字更可信，也更适合 AI 产品。

### 9.5 证据包导出

允许用户导出 Markdown / PDF / JSON：

- 房源摘要。
- 风险和原文依据。
- 追问清单。
- 看房勾选和现场备注。
- 合同风险。
- 最终决定及理由。

导出前自动脱敏手机号、身份证号、微信号和门牌号。这个能力对真实租房场景非常有价值，也适合比赛展示。

### 9.6 离线看房模式

看房页面做成轻量 PWA：提前缓存清单，现场无网也能勾选和记录，恢复网络后再同步。它能把产品从“桌面演示工具”提升成真实现场工具。

### 9.7 下一步行动引擎

工作台不要只显示统计数字，而是给出一个明确的下一步：

- 画像不完整：先补预算和通勤目标。
- 候选少于 2 个：继续扫描房源。
- 有高风险候选：先核验身份和付款规则。
- 看房清单未完成：不要进入签约。
- 合同存在高风险：先索要修改版再决定。

这会明显增强首页的产品价值。

## 10. 推荐实施顺序

### 第一阶段：可信度和可用性

1. 修正图片和链接的能力文案，或真正接入图片 / 链接处理。
2. 接入候选管理页面，或者清理死代码并明确由对比页承担管理职责。
3. 把评分拆成风险、匹配度、完整度和可信度。
4. 为 API 增加输入 schema、输出上限和更可靠的限流。
5. 修复 Render Blueprint 路径、Node 版本和环境文件冲突。

### 第二阶段：稳定的产品底座

1. 抽离 `App.tsx` 的状态和页面路由。
2. 统一 localStorage 适配层、版本迁移和级联删除。
3. 增加证据对象和风险状态。
4. 增加本地规则、存储和评分测试。
5. 增加 Playwright 流程测试和无障碍检查。
6. 清理 CSS 覆盖层，建立真正的组件令牌。

### 第三阶段：可被记住的产品亮点

1. 决策档案。
2. 真实月成本和现金流计算。
3. 证据包导出。
4. OCR / 视觉分析。
5. 离线看房模式。
6. 可选的跨设备同步和隐私安全分享。

## 11. 审查后的最终评价

这个项目最值得保留的不是“AI”三个字，而是“把租房判断拆成可验证步骤”的产品思想。它已经具备一个不错的核心叙事：先明确自己的约束，再识别风险，再把风险带到现场和合同里。

下一步最应该做的是减少表面能力和真实能力之间的落差，把每个结论都绑定到证据、缺失信息和下一步行动。只要完成这一层，项目会从“功能完整的 AI Demo”变成“有可信方法论的租房决策工具”。

## 12. P0 实施验收记录

- 多模态：图片经过浏览器压缩后随请求发送，服务端构造 `image_url` 消息；Qwen 视觉模型默认由 `QWEN_VL_MODEL` 配置，默认值为 `qwen-vl-plus`。
- 候选管理：已新增主导航入口，并接通候选列表、删除和清空操作。
- 评分：关键字段缺失时显示“待补充”，不参与可靠排名，并展示缺失字段。
- API 安全：已增加 JSON Content-Type、字段类型、图片 data URL、图片数量、候选数量和请求大小校验。
- 验收：TypeScript 编译、Vite 生产构建、AI Mock 契约测试、图片消息格式测试和本地 HTTP 接口测试均通过。
