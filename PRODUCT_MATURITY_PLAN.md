# 租房雷达产品成熟化方案

文档状态：实施基线

基线日期：2026-07-17

适用范围：当前 React + Vite + Node.js 项目
目标版本：V2.0 可长期使用版

## 1. 产品结论

租房雷达真正有价值的不是“用 AI 扫描房源”，而是把一次高风险、信息不完整的租房决策拆成可验证的步骤，并保留每一步的依据。

成熟版产品不应继续表现为多个相互独立的小工具。它应围绕一个核心对象组织全部能力：

> 一套候选房源对应一份持续更新的租房决策档案。档案记录用户约束、原始材料、风险证据、待核验事项、真实成本、沟通记录、看房结论、合同风险和最终决定。

产品的最终承诺是：帮助用户减少遗漏、识别不确定性、保留证据并做出可解释的决定。产品不证明房源真实，不提供法律结论，不替用户付款、发消息或签约。

## 2. 目标用户与关键任务

### 2.1 核心用户

- 第一次在陌生城市独立租房的毕业生和年轻职场人。
- 缺少当地经验，需要同时处理预算、通勤、房源真实性和合同风险的租客。
- 已经从多个平台收集房源，但难以比较和持续跟进的人。

### 2.2 用户真正要完成的任务

1. 明确自己的硬约束，避免在看房过程中被低价或话术带偏。
2. 把零散房源材料转成可以核验的事实、风险和问题。
3. 判断哪些房源信息足够完整，可以进入可靠比较。
4. 在线上发现风险后，在线下看房时完成针对性核验。
5. 在付款和签约前确认真实成本、责任边界和退出成本。
6. 保留做出选择的依据，并能导出、复盘或与可信的人讨论。

## 3. 当前基线与成熟度差距

### 3.1 已具备的可信基础

- 画像、扫描、候选、对比、看房、沟通和合同检查已形成完整功能链。
- 本地规则始终可用，AI 作为补充层，失败时不覆盖本地结果。
- 图片输入已经进入真实视觉模型链路，Mock 模式会明确说明能力边界。
- 候选缺少月成本或通勤时会被标记为不可可靠比较。
- 服务端已有请求校验、脱敏、请求 ID、超时、基础限流和 Mock 合约测试。

### 3.2 当前仍有明显 Demo 感的地方

| 领域 | 当前表现 | 产品影响 |
| --- | --- | --- |
| 产品主线 | 九个页面分别完成局部任务，没有统一的决策档案 | 用户不知道一条风险如何流转到看房、合同和最终决定 |
| 首页 | 展示“候选房源样例”“高风险样例”和静态闭环说明 | 像演示看板，不是基于用户数据的工作台 |
| 导航 | 只用 React 内存切换页面，刷新、后退、分享都会丢失位置 | 无法形成可靠应用体验 |
| 数据 | 多个模块直接读写裸 localStorage，无统一异常和迁移策略 | 容量异常、坏数据和删除残留不可控 |
| 候选删除 | 删除房源不会级联清理看房进度和备注 | 会留下孤儿数据 |
| 决策结果 | 有分数，但缺少统一的可信度、证据状态和版本说明 | 用户容易把算法分数误解为事实判断 |
| 成本判断 | 只允许手动填写月租、总月成本和首期成本 | 没有解释费用构成，难以发现“低租金、高附加费” |
| 现场闭环 | 清单能勾选和记备注，但没有形成房源结论 | 看房之后仍需用户自己重新整理 |
| 合同闭环 | 合同检查与候选房源无关联 | 无法证明签约前风险是否被解决 |
| 内容与视觉 | 存在“当前阶段”“闭环流程就绪”等开发状态文案，CSS 有多轮覆盖层 | 暴露开发过程，维护和视觉一致性都较差 |
| 发布质量 | 构建有 NODE_ENV 警告，缺少前端单测、端到端测试和 CI | 不能证明长期迭代不会破坏关键流程 |

## 4. 产品结构

### 4.1 核心对象：租房决策档案

每份档案至少包含：

```text
决策档案
├─ 用户约束快照
├─ 房源原始材料和来源
├─ 已确认事实
├─ 风险证据
│  ├─ 规则或模型来源
│  ├─ 原文或图片依据
│  ├─ 核验状态
│  └─ 下一步动作
├─ 真实成本
├─ 沟通草稿与已确认事项
├─ 看房清单、证据和现场结论
├─ 合同检查记录
└─ 最终决定与理由
```

风险核验状态统一为：`待核验`、`已确认`、`已解释`、`不接受`、`不适用`。

### 4.2 信息架构

一级导航收敛为五个用户任务，工具能力进入档案上下文：

1. 工作台：显示真实进度、阻塞项和唯一的首要下一步。
2. 房源：导入房源、管理候选、查看单套决策档案。
3. 比较：只比较信息足够的房源，并解释差异和不确定性。
4. 看房：按候选生成现场任务，离线记录并形成结论。
5. 签约：沟通审校、合同检查和签约前门槛。

需求画像进入全局设置；演示数据进入帮助菜单，不再占用一级导航。

现有路由在迁移期保留，避免一次性破坏全部页面。V2.0 完成前通过 URL hash 提供可刷新、可后退的稳定地址。

## 5. 核心业务规则

### 5.1 阶段门槛

- 没有完整画像：允许浏览产品，但不生成个性化推荐。
- 没有候选：下一步只能是导入房源。
- 候选缺少月总成本或通勤：不得进入可靠排名。
- 存在未核验的高风险：不得显示“建议签约”。
- 看房关键项未完成：签约阶段显示阻塞提示。
- 合同存在高风险或关键信息缺失：只能显示“需要修改或补充”，不得给出安全结论。

### 5.2 分数语义

- `riskScore`：输入材料在本地规则下的风险基础分，不等于真实性。
- `fitScore`：费用、通勤和偏好与画像的匹配程度。
- `completenessScore`：参与判断所需字段的完整度。
- `confidence`：结果的证据充分程度，不由模型自行声称。
- `decisionScore`：只在关键字段齐全时生成，用于排序，不作为签约建议。

页面必须同时显示分数含义、缺失字段和算法版本。不可比较的房源不显示伪精确总分。

### 5.3 AI 边界

- AI 只分析用户主动提交的材料。
- AI 输出必须带模型、Prompt 版本、请求 ID、时间和输入模态。
- AI 与规则不一致时并列展示，不自动覆盖。
- Mock 输出必须显著标明演示数据，不能混同真实分析。
- 任何合同结论都必须说明不构成法律意见。

## 6. 版本范围与优先级

### P0：从 Demo 变成可靠应用

1. 建立 URL 路由、浏览器前进后退和刷新恢复。
2. 把工作台改为真实行动引擎，删除样例指标和开发状态文案。
3. 建立统一、带版本和异常处理的本地存储层。
4. 删除候选时级联清理看房进度、备注和相关档案数据。
5. 加入全局反馈系统，保存失败、删除、导入和 AI 状态都有可访问提示。
6. 修复构建警告，锁定 Node 版本，补充安全响应头。
7. 为评分、存储迁移和核心规则增加自动化测试。
8. 清理 CSS 多轮覆盖，形成单一设计令牌和组件层。

### P1：形成真实决策闭环

1. 新增候选详情和决策档案。
2. 为风险增加证据引用和核验状态。
3. 新增真实月成本与首期现金流计算器。
4. 看房完成后生成结构化结论和待追问清单。
5. 合同检查结果关联候选房源，支持重新检查和差异记录。
6. 沟通助手增加事实、承诺、法律断言和隐私审校。
7. 支持本地数据导出、导入和全量清除。

### P2：跨设备与现场能力

1. PWA 和离线看房模式。
2. 看房照片及证据附件。
3. Markdown、JSON 和 PDF 证据包导出。
4. 可选账户、加密同步和隐私分享链接。
5. 房源网页抓取，在实现 SSRF、内容限制和平台合规后上线。
6. 固定脱敏评估集、模型质量反馈和成本监控。

## 7. 体验与视觉标准

### 7.1 设计判断

Reading this as：面向普通租客的高信任决策工具，界面应克制、清楚、重证据，采用轻量产品工作台而不是营销页或比赛展示页。

设计参数：

- `DESIGN_VARIANCE: 4`，稳定清晰，局部使用非对称信息层级。
- `MOTION_INTENSITY: 3`，只保留状态反馈、页面进入和必要的层级变化。
- `VISUAL_DENSITY: 6`，适合信息型产品，但移动端保持可触控和可扫读。

### 7.2 视觉规则

- 使用一套冷中性灰与克制的绿色强调色，不在页面中随机切换主题。
- 主体圆角、表单圆角和按钮圆角有明确层级，不混用无规则的胶囊与方角。
- 卡片只用于真实分组和层级，不把每个数字都放进悬浮白卡。
- 数字启用等宽数字；金额、分钟和分数对齐。
- 删除版本号、开发阶段、示例统计和“功能已就绪”等过程文案。
- 所有按钮包含 hover、pressed、disabled 和 focus-visible 状态。
- 加载使用与内容形状一致的骨架；空状态必须告诉用户下一步。
- 390px 宽度无横向溢出；触控目标不小于 44px。
- 支持 `prefers-reduced-motion`；正文和表单达到 WCAG AA 对比度。
- 真实产品页面不展示无意义装饰动画、滚动提示、假精确指标或 AI 风格渐变。

## 8. 工程标准

- 保留 React + Vite + TypeScript 和现有 Node 服务，不迁移框架。
- 页面、应用状态、存储和业务规则分层，`App.tsx` 不继续承载全部业务动作。
- 第三方依赖引入前必须检查现有依赖和必要性。
- 本地数据结构带 schema version，并提供逐版本迁移。
- 服务端输入和输出都限制字段长度、数组数量和总大小。
- 静态文件必须使用基于 `resolve` 和目录边界的路径校验。
- 生产响应包含 CSP、nosniff、Referrer-Policy 和 Permissions-Policy。
- 日志不记录完整合同、聊天原文、图片 data URL 或个人身份信息。

## 9. 验收矩阵

### 9.1 产品验收

- 新用户进入后不会看到伪造的个人数据或样例统计。
- 首页始终根据当前真实数据给出一个明确的首要行动。
- 用户可以从画像、导入房源、核验风险、比较、看房到合同检查完成一条可恢复流程。
- 每个风险都能回答：依据是什么、谁给出的、是否核验、下一步是什么。
- 缺失关键信息时系统明确阻止可靠排名和签约建议。
- 删除候选后不存在对应的看房进度、备注或档案孤儿数据。
- 用户可以导出和清除自己的本地数据。

### 9.2 交互验收

- URL、刷新、后退和前进可以恢复当前页面。
- 所有表单有标签、错误提示和键盘焦点。
- 所有异步操作覆盖 idle、loading、success、empty 和 error。
- 桌面、平板和 390px 手机视口无横向溢出。
- 删除、清空和覆盖导入有明确确认，并说明影响范围。

### 9.3 工程验收

- `npm run build` 无警告并通过。
- 本地规则、评分、存储迁移和 API Mock 测试通过。
- 真实浏览器端到端验收覆盖一条完整租房决策流程和一个错误降级流程，并保留可复核记录。
- 浏览器控制台无错误，核心页面无未捕获异常。
- Lighthouse 目标：Accessibility 不低于 95，Best Practices 不低于 95，CLS 小于 0.1。
- 依赖审计无高危漏洞，生产环境不暴露密钥和内部 Prompt。

## 10. 完成定义

只有在以下条件同时满足时，才能将“产品成熟化”标记为完成：

1. P0 全部完成并有自动化或可复核证据。
2. P1 的决策档案、证据状态、真实成本、看房结论、合同关联和数据导出全部可用。
3. 关键流程通过桌面与移动端真实浏览器验收。
4. 页面中不存在比赛占位、版本标签、开发阶段、无效按钮和死入口。
5. 文档、当前代码、测试结果和部署行为一致。
6. 生产构建、测试、浏览器控制台和安全边界均通过最终审计。

P2 中的账户同步和网页抓取属于需要额外基础设施与合规评估的增强项，不阻塞单设备成熟版交付；如果对外宣称支持这些能力，则必须完成相应验收后才能发布。

## 11. 实施记录

本节在每轮开发结束后更新，记录实际完成项、证据和未通过项。未在此处登记并验收的功能不视为完成。

| 日期 | 范围 | 状态 | 验收证据 |
| --- | --- | --- | --- |
| 2026-07-17 | 产品与工程基线审计 | 已完成 | 代码、数据流、服务端、安全边界和桌面/移动端基线已检查 |
| 2026-07-17 | 稳定导航与真实工作台 | 已完成 | Hash 地址支持刷新、前进和后退；工作台只展示真实画像与候选数据，并给出唯一下一步 |
| 2026-07-17 | 本地数据可靠性 | 已完成 | 画像与候选升级为带版本存储；兼容旧数据；删除候选级联清理；存储异常有全局提示 |
| 2026-07-17 | 风险证据与真实成本 | 已完成 | 风险带证据、来源、核验状态和下一步；对比页计算月总成本与首期现金流；相关单元测试通过 |
| 2026-07-17 | 看房现场结论 | 已完成 | 每套候选支持继续、暂缓、淘汰或待定结论；同步显示清单完成度和未解决风险；刷新后可恢复 |
| 2026-07-17 | 合同关联与历史 | 已完成 | 合同扫描可关联候选并保存最近 10 次原文和结果；真实浏览器刷新后记录仍可恢复 |
| 2026-07-17 | 数据可携带性 | 已完成 | JSON 导出、覆盖导入和全量清除均有验证与确认；看房结论和合同历史随备份迁移 |
| 2026-07-17 | 工程门禁 | 已完成 | Node 版本锁定；生产构建无警告；安全响应头已核验；GitHub Actions 覆盖安装、核心测试、AI Mock 和构建 |
| 2026-07-17 | 沟通安全与决策档案 | 已完成 | 本地与 AI 回复均检查新增金额、付款承诺、法律断言和隐私信息；候选页汇总成本、现场结论和合同状态；跨页面自动带入候选上下文 |
| 2026-07-17 | 设计系统收敛 | 已完成 | 合并重复设计令牌；移除外部字体请求、全站渐变、无意义悬浮和首屏隐藏动画；保留单一冷灰与深绿色视觉体系 |
| 2026-07-17 | 最终质量验收 | 已完成 | 25 项核心测试、AI Mock 合约测试与生产构建通过；依赖审计 0 漏洞；9 个页面在桌面和 390px 手机视口无横向溢出、空按钮、无标签控件或失效图片；文本对比度达到 WCAG AA；Lighthouse 桌面端 Performance、Accessibility、Best Practices 均为 100，CLS 为 0，FCP/LCP 约 620ms |
