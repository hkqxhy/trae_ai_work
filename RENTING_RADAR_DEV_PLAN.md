# 租房雷达开发计划

> 本文档是本项目后续开发的工作入口。每次 Codex 开始工作前，应先阅读本文件，确认当前阶段、单次任务范围、验收标准和用户需要配合的事项。

## 1. 项目定位

项目名称：租房雷达

赛道：生活娱乐

一句话定位：面向陌生城市租房者的 AI 决策助手，帮助用户从区域选择、房源识别、候选对比、看房排雷到合同风险检查，降低租房过程中的信息不对称和决策焦虑。

核心原则：本产品不是房源交易平台，也不替用户做最终决定，而是帮助用户把租房信息变成可验证、可比较、可解释的决策依据。

## 2. 当前产物

当前已完成：

- `renting-radar-entry.html`：报名用单文件创意展示页，包含项目介绍、流程说明、房源风险扫描交互 Demo 和价值说明。

当前未完成：

- 可维护的正式应用工程。
- 真实 AI 接口接入。
- 房源数据结构化存储。
- 区域推荐、房源库、候选对比、看房清单、合同分析等正式功能。

## 3. 总体开发策略

采用小步迭代方式开发。每次更新只做一个功能点，不跨功能混改，避免互相干扰。

每次任务必须遵守：

- 开始前先阅读本文档。
- 只修改本轮任务相关文件。
- 不做无关重构。
- 不改变已完成功能的行为，除非当前任务明确要求。
- 每个功能点完成后更新本文档的进度状态。
- 每个功能点都要有明确验收标准。
- 如果需要用户提供信息，先在本文档的“用户配合事项”中标明。

## 4. 建议技术路线

正式开发建议采用 Web App 形态。

推荐路线：

- 前端：React + Vite 或 Next.js。
- 样式：CSS Modules、Tailwind 或项目内统一 CSS，保持简洁一致。
- 数据：早期使用本地状态和浏览器 localStorage，后期再接数据库。
- AI 能力：先做模拟规则与 Prompt 结构，之后再接入大模型 API。
- 文件处理：合同和截图分析先做文本输入版，后续再扩展 OCR、图片上传和文档解析。

如果用户希望优先快速参赛展示，可以先继续使用单 HTML 原型增强；如果用户希望做成可扩展产品，则应先搭建正式工程。

## 5. 版本路线图

### V0.1 报名展示版

目标：完成比赛报名需要的创意展示材料。

状态：已完成。

已包含：

- 创意介绍。
- 用户痛点。
- 产品流程。
- 房源风险扫描模拟 Demo。
- 价值说明。

验收标准：

- HTML 文件可直接打开。
- 报名帖文案满足赛道格式。
- 创意能被评审在 3 分钟内理解。

### V0.2 正式应用骨架

目标：把展示页升级为可持续开发的应用基础。

状态：已完成。

单次任务拆分：

1. 搭建前端工程。
2. 建立基础页面路由。
3. 迁移视觉风格与基础布局。
4. 建立全局类型与数据模型。

已完成内容：

- 新增 React + Vite + TypeScript 正式前端工程。
- 新增工作台、需求画像、房源扫描、候选对比、看房清单基础页面入口。
- 迁移 `renting-radar-entry.html` 的绿色可信赖视觉风格，形成应用式侧边导航与内容区布局。
- 新增租房需求、房源、费用、风险、路由等全局类型与示例数据。

验收结果：

- `npm run build` 通过。
- 本地开发服务 `http://127.0.0.1:5173` 返回 200。
- 当前版本未接入 AI，也未实现 V0.3 以后的具体业务功能。

### V0.3 租房需求画像

目标：让用户输入城市、预算、通勤地点和偏好，生成租房需求画像。

状态：已完成。

单次任务拆分：

1. 新增需求问卷表单。
2. 新增需求数据保存功能。
3. 新增租房画像摘要展示。
4. 新增偏好优先级排序。

已完成内容：

- 新增租房需求画像表单，支持填写目标城市、通勤目标、预算范围、最长通勤时间和居住方式。
- 新增偏好与优先级编辑，支持添加、删除偏好，并按“必须满足、比较重要、可以妥协”排序展示。
- 新增本地保存能力，画像数据保存到浏览器 localStorage。
- 新增画像摘要与系统建议，基于当前画像生成可解释的租房决策基线。
- 工作台已读取当前画像，展示目标城市、通勤上限和关键偏好摘要。

验收结果：

- `npm run build` 通过。
- `npm audit` 通过，显示 0 vulnerabilities。
- 本地开发服务 `http://127.0.0.1:5173` 返回 200。
- 内置浏览器工具当前不可用，未完成自动化可视化点击验证；需要用户在页面中手动确认表单保存、重置和刷新后保留数据。

### V0.4 房源风险扫描

目标：用户粘贴房源描述后，系统输出可信度评分、风险点和追问建议。

状态：已完成。

单次任务拆分：

1. 新增房源文本输入功能。
2. 新增规则版风险识别器。
3. 新增风险评分展示。
4. 新增追问清单生成。
5. 接入 AI 版风险分析。

已完成内容：

- 新增房源扫描页面，支持粘贴房源描述、平台文案或聊天片段。
- 根据用户反馈扩展为多模态扫描入口，支持文字、链接、图片三种输入模式。
- 链接输入支持识别小红书、豆瓣、58 同城、链家、自如、安居客等来源，并给出社交平台房源二次核验建议。
- 图片输入支持上传多张房源截图或聊天截图，生成图片证据记录、图片核验建议和多模态 Prompt。
- 新增本地规则版风险识别器，覆盖提前收定金、催促决策、地址不透明、公寓水电、合同不透明、发布者身份不明、隔断暗间等风险。
- 新增可信度评分、风险等级、扫描结论和行动建议。
- 新增建议追问清单，帮助用户在约看、转账或签约前确认关键问题。
- 扫描结果已结合 V0.3 租房画像，围绕用户预算、目标城市和必须满足项给出解释。
- 新增 AI 接入准备区，生成结构化 Prompt 草稿；真实外部 AI API 暂未接入，需用户确认密钥和模型后再开发。

验收结果：

- `npm run build` 通过。
- `npm audit` 通过，显示 0 vulnerabilities。
- 本地开发服务 `http://127.0.0.1:5173` 返回 200。
- 内置浏览器工具当前不可用，未完成自动化可视化点击验证；需要用户在页面中手动确认文字、链接、图片三种模式，高风险示例、稳妥示例、小红书示例、空输入、图片上传预览和 Prompt 展开收起。

### V0.5 候选房源管理

目标：让用户保存多个候选房源，并统一查看。

状态：已完成。

单次任务拆分：

1. 新增房源卡片数据结构。
2. 新增保存候选房源功能。
3. 新增候选房源列表。
4. 新增删除候选房源功能。
5. 新增本地持久化。

已完成内容：

- 新增候选房源数据结构，保存扫描输入、扫描结果、风险评分、来源、备注和创建时间。
- 新增本地持久化能力，候选房源保存到浏览器 localStorage。
- 房源扫描页新增“保存为候选”功能，支持为候选房源添加备注。
- 候选房源页支持统一查看已保存房源，展示来源、输入类型、风险评分、风险点数量、备注和关键风险。
- 候选房源页支持删除单个候选房源和清空全部候选房源。

验收结果：

- `npm run build` 通过。
- 本地开发服务 `http://127.0.0.1:5173` 返回 200。
- 当前版本只做候选保存与管理，不做 V0.6 的综合对比算法和决策解释。

### V0.6 多房源对比

目标：将候选房源放在同一张对比表中，辅助用户做决策。

状态：已完成。

单次任务拆分：

1. 新增对比表页面。
2. 新增通勤、费用、风险字段展示。
3. 新增综合推荐分。
4. 新增 AI 决策解释。

已完成内容：

- 新增多房源对比页面，基于 V0.5 已保存候选房源生成对比表。
- 新增候选房源可编辑对比字段：月租、总月成本、首期成本、通勤时间、地铁距离。
- 新增综合推荐分，结合风险评分、预算匹配、通勤匹配和信息完整度进行本地计算。
- 新增决策解释卡片，说明每个候选房源在费用、通勤、风险和信息完整度上的取舍。
- 新增当前优先候选展示，按综合推荐分排序。
- 对比字段会写回浏览器 localStorage，刷新后保留。

验收结果：

- `npm run build` 通过。
- 本地开发服务 `http://127.0.0.1:5173` 返回 200。
- 当前版本使用本地规则生成决策解释，未接入真实 AI API。

### V0.7 看房排雷清单

目标：根据某个房源自动生成线下看房检查清单。

状态：已完成。

单次任务拆分：

1. 新增通用看房清单。已完成。
2. 新增基于风险点的定制清单。已完成。
3. 新增看房时勾选状态。已完成。
4. 新增看房备注记录。已完成。

本轮已完成内容：

- 新增通用看房清单数据，覆盖身份与出租权、费用与付款、房屋状况、环境与通勤、合同与证据五类检查。
- 每个检查项目均包含现场检查方法和异常信号。
- 新增通用看房清单页面，展示项目数量、分类数量和建议检查用时。
- 新增候选房源选择器，可选择一个已保存候选房源生成定制清单。
- 新增风险到现场检查项的转换规则，将定金、身份、地址、合同、费用、隔断和图片等风险转换为具体核验方法。
- 未识别的风险会生成兜底核验项目，确保扫描风险不会遗漏。
- 新增通用和定制清单的现场勾选状态。
- 新增完成数量和完成百分比展示，并支持重置当前进度。
- 勾选进度按候选房源保存到浏览器 localStorage，切换候选时互不干扰。
- 新增现场总结和待追问事项两类看房备注。
- 备注按候选房源保存到浏览器 localStorage，支持保存时间提示和清空操作。

验收结果：

- `npm run build` 通过。
- `npm audit` 通过，显示 0 vulnerabilities。
- 本地开发服务 `http://127.0.0.1:5173` 返回 200。

### V0.8 聊天式谈判助手

目标：根据中介或房东话术，生成合适回复。

状态：进行中（3/4，API 暂缓）。

单次任务拆分：

1. 新增话术输入框。已完成。
2. 新增回复风格选择。已完成。
3. 新增规则版回复生成。已完成。
4. 接入 AI 版回复生成。按用户要求暂缓。

本轮已完成内容：

- 新增“沟通助手”导航入口。
- 新增对方原话输入框和沟通目标输入框。
- 新增催定金、费用条款两个示例输入。
- 新增字符统计、输入完整度提示和清空功能。
- 新增温和沟通、坚定边界、继续追问三种回复风格。
- 新增当前风格说明和表达原则展示。
- 新增本地规则场景识别，覆盖定金、费用、合同、身份、提前退租和一般沟通。
- 新增结合回复风格的可编辑建议回复、识别信号和待确认问题。

本轮未包含：

- 不接入 AI 回复。

验收结果：

- `npm run build` 通过。
- `npm audit` 通过，显示 0 vulnerabilities。
- 本地开发服务 `http://127.0.0.1:5173` 返回 200。

### V0.9 合同风险检查

目标：用户粘贴合同条款或费用明细后，系统标出风险条款。

状态：规则版已完成（4/5，AI 暂缓）。

单次任务拆分：

1. 新增合同文本输入。已完成。
2. 新增押金与违约条款识别。已完成。
3. 新增费用明细识别。已完成。
4. 新增维修责任识别。已完成。
5. 接入 AI 合同风险总结。按用户要求暂缓。

本轮已完成内容：

- 新增“合同检查”导航入口。
- 新增合同条款、费用明细和补充背景输入。
- 新增风险条款示例、字符统计、输入状态和清空功能。
- 新增本地押金与违约条款扫描器，识别押金不退、模糊扣款、剩余租期违约金、多月租金违约金、按日累计和单方解除等风险信号。
- 新增押金退还时限和违约责任计算方式完整性检查。
- 新增结构化扫描结果，展示风险等级、原文依据、判断理由、识别摘要和签约前建议追问。
- 新增高风险与相对清晰两组合同示例。
- 新增租金外费用识别，覆盖金额未明确的物业费、管理费、服务费、网络费和保洁费等项目。
- 新增商水商电、公摊费用、单方调价和兜底式“其他费用”风险识别。
- 新增费用项目摘要，集中展示合同中已出现的租金外费用名称。
- 新增维修责任识别，覆盖全部维修转嫁、房屋本体与隐蔽工程转嫁、出租方完全免责、紧急维修处理受限和模糊归责。
- 新增维修范围摘要，展示合同中提及的房屋本体、家具家电、管线、防水和门锁等项目。
- 相对清晰示例补充自然损耗、人为损坏和紧急故障响应时限的责任划分。

验收结果：

- `npm run build` 通过。
- `npm audit` 通过，显示 0 vulnerabilities。
- 已验证空输入不会触发扫描。
- 已验证高风险示例可识别押金不退、剩余租期违约金和押金退还时限缺失。
- 已验证相对清晰示例不会匹配明显高风险规则。
- 已验证窄屏下结果卡片改为单列显示。
- 已验证高风险示例可识别未明确金额的额外费用、商业水电和单方调整收费标准。
- 已验证明确金额、居民水电且无额外服务费的示例不会触发费用风险。
- 已验证高风险示例可识别“全部维修费用由承租方承担”。
- 已验证区分自然损耗与人为损坏、明确出租方响应时限的示例不会触发维修风险。

### V1.0 参赛演示版

目标：形成可以稳定演示的完整闭环。

状态：已完成。

必须包含：

- 需求画像。
- 房源风险扫描。
- 候选房源保存。
- 多房源对比。
- 看房清单。
- 简短演示脚本。

当前已完成：

- 新增“演示指南”导航入口。
- 新增 3 分钟答辩时间线，覆盖产品定位、需求画像、房源扫描、候选对比、看房清单和合同检查。
- 每个步骤包含现场操作、讲解话术、验收信号和页面直达按钮。
- 新增演示准备状态，提示当前候选房源数量是否适合直接演示对比流程。
- 页面总标题会随当前功能显示对应版本和可用状态，避免演示时出现阶段信息错位。

验收结果：

- `npm run build` 通过。
- `npm audit` 通过，显示 0 vulnerabilities。
- 已自动验证需求画像可保存到 localStorage。
- 已自动验证稳妥与高风险房源均可扫描并保存，共形成 2 个候选房源。
- 已自动验证对比页展示 2 个候选、优先候选和 2 份决策解释。
- 已自动验证看房页加载 2 个候选并生成风险定制检查项。
- 已自动验证合同页同时输出押金、违约、费用与维修类别提示。
- 已自动验证演示指南包含 6 个步骤和 6 个页面直达按钮，并显示“候选数据已就绪”。
- 已自动验证 390px 窄屏无横向溢出，导航和演示步骤保持单列可读。

验收标准：

- 用户可以从输入租房需求开始，完成一次完整租房决策流程。
- 页面无明显布局错乱。
- 核心功能在演示环境中可稳定运行。
- 有一份 3 分钟答辩演示流程。

## 6. 单次开发任务模板

每次开始新任务时，Codex 应按以下格式确认任务：

```markdown
## 本轮任务

功能点：

目标：

涉及文件：

不做的事情：

验收标准：

用户需要提供：
```

每次完成任务后，Codex 应更新：

- 对应路线图状态。
- 完成内容。
- 验收结果。
- 遗留问题。

## 7. 工作规范

### 范围控制

- 一次只做一个功能点。
- 不在同一轮同时做 UI 重设计、数据结构调整和 AI 接口接入。
- 如果发现必须改动共享结构，先说明原因，再执行最小改动。

### 文件管理

- 新功能应尽量放在独立模块或独立组件中。
- 不删除用户已有文件，除非用户明确要求。
- 不覆盖用户手动修改的内容。
- 每次新增文件要有清晰命名。

### 体验规范

- 产品界面应服务真实租房决策，不做空泛营销页。
- 每个输出都要给出理由，而不是只给结论。
- 风险提示要克制，不夸大，不制造恐慌。
- 合同分析、法律相关内容必须表达为风险提示和追问建议，不作为法律意见。

### AI 规范

- 先实现可控的规则版或模拟版，再接入 AI。
- AI 输出必须结构化，至少包含：结论、理由、风险点、建议追问。
- 不把 AI 判断包装成绝对事实。
- 对图片、链接、合同等输入，要提示用户结果依赖输入信息完整度。

### 测试规范

每个功能至少验证：

- 正常输入。
- 空输入。
- 高风险输入。
- 移动端布局或窄屏显示。

如果暂时无法自动化测试，必须在完成说明中写明手动验证方式。

## 8. 用户配合事项

用户可以通过以下方式帮助项目更快成型：

### 需要用户尽早决定

1. 正式应用形态：继续做单 HTML 原型，还是升级为 React/Next.js 工程。
2. 目标演示范围：只做报名展示，还是做可操作的参赛 Demo。
3. 优先功能：区域推荐、房源扫描、候选对比、看房清单、合同分析中先做哪一个。
4. 是否接入真实 AI API。

### 用户可以提供的材料

1. 目标租房城市。
2. 预算范围。
3. 工作或学校位置。
4. 真实或模拟房源文案。
5. 与中介或房东的聊天片段，注意先删除手机号、姓名、门牌号等隐私。
6. 常见租房合同条款截图或文本，注意先删除个人信息。
7. 希望参赛时重点展示的场景。

### 用户反馈方式

每次看完阶段成果后，建议按下面格式反馈：

```markdown
保留：
修改：
删除：
下一步优先做：
```

这样可以减少反复沟通成本，也能让每次迭代保持单一目标。

## 9. 当前推荐下一步

推荐下一步：使用真实脱敏材料进行一次参赛前试演。

原因：当前非 AI 路线图已经完成，下一步更有价值的是用真实或模拟的脱敏房源、聊天和合同材料校准规则命中效果与 3 分钟讲解节奏。AI 接入仍按用户要求暂缓。

如果用户只想快速完善参赛附件，则下一步应改为：增强 `renting-radar-entry.html` 的房源风险扫描 Demo。

## 10. 进度记录

| 日期 | 版本 | 状态 | 内容 |
| --- | --- | --- | --- |
| 2026-06-17 | V0.1 | 已完成 | 生成报名帖文案与 `renting-radar-entry.html` 创意展示页 |
| 2026-06-17 | 计划文档 | 已完成 | 新增本开发计划文档，明确路线图、工作规范和用户配合事项 |
| 2026-06-17 | V0.2 | 已完成 | 搭建 React + Vite + TypeScript 正式应用骨架，新增基础页面路由、视觉布局、全局类型与示例数据，并完成构建和本地访问验收 |
| 2026-06-17 | V0.3 | 已完成 | 新增租房需求画像表单、本地保存、画像摘要、偏好优先级排序和工作台画像摘要，并完成构建、审计和本地访问验收 |
| 2026-06-18 | V0.4 | 已完成 | 新增房源风险扫描页面、本地规则识别器、可信度评分、风险点、追问清单、画像关联解释和 AI Prompt 预留，并完成构建、审计和本地访问验收 |
| 2026-06-18 | V0.4 扩充 | 已完成 | 根据用户反馈将房源扫描扩展为多模态入口，支持文字、链接、图片输入，新增平台识别、图片证据预览、多模态处理说明和多模态 Prompt |
| 2026-06-18 | V0.5 | 已完成 | 新增候选房源数据结构、本地持久化、扫描页保存候选、候选房源列表、删除单项和清空全部功能，并完成构建和本地访问验收 |
| 2026-06-18 | V0.6 | 已完成 | 新增多房源对比表、费用与通勤字段编辑、综合推荐分、优先候选展示和本地决策解释，并完成构建和本地访问验收 |
| 2026-06-18 | V0.7-1 | 已完成 | 新增五类通用看房清单，提供检查方法和异常信号；未包含风险定制、勾选状态和备注记录 |
| 2026-06-18 | V0.7-2 | 已完成 | 新增候选房源选择和风险到现场检查项的转换规则，生成房源定制看房清单；未包含勾选状态和备注 |
| 2026-06-18 | V0.8-1 | 已完成 | 新增沟通助手入口、对方原话和沟通目标输入、示例加载、字符统计及清空功能；未包含风格选择和回复生成 |
| 2026-06-18 | V0.7-3 | 已完成 | 新增通用与定制清单勾选状态、完成进度、重置操作和按候选房源的 localStorage 持久化；未包含看房备注 |
| 2026-06-18 | V0.7-4 | 已完成 | 新增按候选房源保存的现场总结、待追问事项、保存时间提示和清空操作，完成 V0.7 看房清单闭环 |
| 2026-06-18 | V0.8-2 | 已完成 | 新增温和沟通、坚定边界和继续追问三种回复风格，并展示当前风格说明和表达原则；未包含回复生成 |
| 2026-06-18 | V0.8-3 | 已完成 | 新增定金、费用、合同、身份、退租等本地场景识别，结合风格生成可编辑回复和待确认问题；AI 回复暂缓 |
| 2026-06-18 | V0.9-1 | 已完成 | 新增合同检查入口、合同条款与补充背景输入、风险示例、字符统计及清空功能；未包含条款识别 |
| 2026-06-18 | V0.9-2 | 已完成 | 新增押金与违约条款本地规则识别、完整性检查、原文依据、风险理由和建议追问；费用、维修与 AI 总结仍待后续独立迭代 |
| 2026-06-18 | V0.9-3 | 已完成 | 新增租金外费用、商业水电、公摊费用、单方调价和兜底收费识别，并在摘要中展示合同已出现的费用项目 |
| 2026-06-18 | V0.9-4 | 已完成 | 新增维修责任转嫁、出租方免责、紧急维修受限和模糊归责识别，完成 V0.9 合同检查本地规则版 |
| 2026-06-18 | V1.0-1 | 已完成 | 新增演示指南导航与 3 分钟答辩时间线，串联需求画像、房源扫描、候选对比、看房清单和合同检查 |
| 2026-06-18 | V1.0 | 已完成 | 完成画像到合同检查的自动化闭环验收、窄屏验收和页面阶段状态修正，形成可稳定演示的规则版应用 |
